name: ci

on:
  push:
    branches:
      - master

jobs:
  ml:
    uses: xundaoxd/docker-apps/.github/workflows/scripts/action.yml@master
    with:
      dockerfile: dockerfiles/ml/Dockerfile
      tag: xundaoxd/ml:latest
    secrets: inherit

